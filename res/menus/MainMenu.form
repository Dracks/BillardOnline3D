form particleEditor
{
    theme = res/theme/editor.theme
    autoWidth = true
    autoHeight = true

    container presets
    {
        style = basic
        layout = LAYOUT_VERTICAL
		alignment = ALIGN_VCENTER_HCENTER
		width = 280
		height = 250

		label title
        {
            style = title
            autoWidth=true
			height=50
            text = Main Menu
        }
		
		button singlePlayer
        {
            style = buttonStyle
            autoWidth = true
            height = 50
            text = Single Player
        }
		
		button multiPlayer
        {
            style = buttonStyle
            autoWidth = true
            height = 50
            text = OnLine Network
        }
		
		button config
        {
            style = buttonStyle
            autoWidth = true
            height = 50
            text = Settings
        }
	}
}